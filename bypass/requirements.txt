# Lambda 実行に必要な最小依存関係
# pyproject.toml の dependencies + lambda グループから生成
# dev グループ（pytest, moto, boto3 等）は含まない
# Windows 専用パッケージ（pywin32, win32_setctime 等）は含まない

fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
python-jose[cryptography]>=3.3.0
mangum>=0.17.0
boto3>=1.34.0
